#include <bits/stdc++.h>
const long long n = 10e9;
using namespace std;

int main()
{
    int sum;

    sum = ((n(n+1))/2)

    cout << sum << endl;

    return 0;
}

/*in the first problem if we wanted to calculate
the sum we will face TLE problem.
but in this program we are able to do the same thing in big-O(1).
which is nothing compared to big-O(10e9).
*/
